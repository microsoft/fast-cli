export interface ComponentTemplateConfig {
    /**
     * The component's HTML tag name. 
     * e.g. my-component
     */
    tagName: string;

    /**
     * The components class
     * e.g. MyComponent
     */
    className: string;
}
